<% if user_signed_in? %>
  <li>
    <%= link_to(I18n.t('layout.logout'), destroy_user_session_path, method: :delete) %>
  </li>
<% else %>
  <li>
    <%= link_to(I18n.t('layout.login'), new_user_session_path) %>
  </li>
<% end %>
